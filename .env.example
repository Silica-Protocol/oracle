# ============================================================================
# Chert PoUW Oracle - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env or any file containing real credentials to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------
CHERT_POI_HOST=127.0.0.1
CHERT_POI_PORT=8765
CHERT_POI_BASE_URL=https://oracle.chert.network

# ----------------------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------------------
CHERT_POI_REQUIRE_HTTPS=true
CHERT_POI_ENABLE_AUTH=true
CHERT_POI_RATE_LIMIT_PER_MINUTE=60

# ----------------------------------------------------------------------------
# Oracle API Key (REQUIRED)
# Generate a secure 32+ character key for production
# ----------------------------------------------------------------------------
CHERT_ORACLE_API_KEY=your_oracle_api_key_here_min_32_chars

# ----------------------------------------------------------------------------
# BOINC Project Authenticators (at least ONE required)
# 
# How to get your authenticator:
# 1. Register/login at the BOINC project website
# 2. Go to Account Settings -> Account Keys (or "Weak Authenticator")
# 3. Copy the 32-character hex string
#
# Only set authenticators for projects you're registered with
# ----------------------------------------------------------------------------

# MilkyWay@Home - https://milkyway.cs.rpi.edu/milkyway/
CHERT_BOINC_MILKYWAY_AUTHENTICATOR=your_milkyway_authenticator_here

# Rosetta@Home - https://boinc.bakerlab.org/rosetta/
# CHERT_BOINC_ROSETTA_AUTHENTICATOR=your_rosetta_authenticator_here

# World Community Grid - https://www.worldcommunitygrid.org/
# CHERT_BOINC_WCG_AUTHENTICATOR=your_wcg_authenticator_here

# GPUGRID - https://www.gpugrid.net/
# CHERT_BOINC_GPUGRID_AUTHENTICATOR=your_gpugrid_authenticator_here

# ----------------------------------------------------------------------------
# Optional: Override default BOINC project URLs (rarely needed)
# ----------------------------------------------------------------------------
# CHERT_BOINC_MILKYWAY_URL=https://milkyway.cs.rpi.edu/milkyway_cgi/cgi
# CHERT_BOINC_MILKYWAY_MASTER_URL=https://milkyway.cs.rpi.edu/milkyway/

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------
CHERT_LOG_LEVEL=info
CHERT_SANITIZE_LOGS=true

# ============================================================================
# IMPORTANT SECURITY NOTES:
# ============================================================================
# 1. NEVER commit real credentials to version control
# 2. Use environment variables or a secrets manager in production
# 3. Authenticators are alphanumeric, typically 32 hex characters
# 4. The Oracle API key must be at least 32 characters
# 5. HTTPS is enforced by default for all external communications
# ============================================================================
